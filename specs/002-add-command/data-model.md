# Data Model: Add Command

## Entities

### Worktree
Same fields as `specs/001-list-command/data-model.md` apply here; `add` operates on creating new Worktree entries on disk via git.

### Repository
Contains top-level path and default ref detection data used by `-b` flow.

# Quickstart: Prune Command

Run `wt prune` from within a Git repository to prune stale worktrees:

```bash
wt prune
```

This delegates to `git worktree prune` and prints success or an error message.
